# 🚀 Настройка GitHub репозитория для Macro-Assistant

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на https://github.com
2. Нажмите кнопку "New repository" (зеленая кнопка)
3. Заполните форму:
   - **Repository name**: `Macro-Assistant`
   - **Description**: `🚀 Универсальный AI-ассистент для автоматизации любых задач на компьютере`
   - **Visibility**: Public (или Private по желанию)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
4. Нажмите "Create repository"

## Шаг 2: Подключение локального репозитория

После создания репозитория на GitHub выполните эти команды в терминале:

```bash
# Добавляем удаленный репозиторий
git remote add origin https://github.com/YOUR_USERNAME/Macro-Assistant.git

# Переименовываем ветку в main (современный стандарт)
git branch -M main

# Пушим код на GitHub
git push -u origin main
```

## Шаг 3: Проверка

После выполнения команд:
1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загрузились
3. Проверьте, что README.md отображается корректно

## Альтернативный способ (через GitHub CLI)

Если у вас установлен GitHub CLI:

```bash
# Создание репозитория и пуш одной командой
gh repo create Macro-Assistant --public --description "🚀 Универсальный AI-ассистент для автоматизации любых задач на компьютере" --push
```

## Готовые команды для копирования:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/Macro-Assistant.git
git branch -M main
git push -u origin main
```

## После успешного пуша

Ваш репозиторий будет доступен по адресу:
`https://github.com/YOUR_USERNAME/Macro-Assistant`

И будет содержать:
- ✅ Полную архитектуру Macro-Assistant
- ✅ Модульную систему
- ✅ Документацию
- ✅ Примеры модулей
- ✅ Инструменты разработки
